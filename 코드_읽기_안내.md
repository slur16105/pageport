# PAGEPORT 코드 읽기 안내

이 문서는 개발자가 아닌 운영자가 PAGEPORT 소스코드의 큰 구조를 이해하기 위한 지도입니다. 코드를 처음부터 끝까지 읽을 필요는 없습니다. 궁금한 기능에 맞는 폴더를 찾고, 각 파일 위쪽의 한국어 주석부터 읽으면 됩니다.

## 가장 먼저 알아둘 점

- `app`은 고객과 관리자가 보는 화면 및 화면에서 호출하는 기능입니다.
- `components`는 여러 화면에서 다시 사용하는 버튼, 헤더, 푸터 같은 부품입니다.
- `lib`은 결제·이메일·다운로드·보안처럼 화면 뒤에서 작동하는 업무 규칙입니다.
- `emails`는 고객에게 보내는 인증·구매·환불 이메일의 내용과 디자인입니다.
- `prisma`는 데이터베이스 표와 보안 규칙의 변경 기록입니다.
- `tests`는 주요 기능이 예상대로 작동하는지 자동으로 확인하는 검사입니다.

## 기능별로 어디를 보면 되나요?

### 메인 화면과 상품 찾기

- `app/page.tsx`: 메인 화면의 큰 구성 순서
- `app/about/page.tsx`: 서비스 운영 원칙과 구매자·관리자 이용 흐름
- `components/HeroSection.tsx`: 첫 화면 문구와 문서 모션
- `components/ProductCatalog.tsx`: 상품 카테고리 필터
- `app/globals.css`: 색상·크기·화면 배치와 모션 디자인

### 상품 구매와 결제

- `app/products/[slug]`: 상품 상세와 이메일 확인
- `app/checkout/[slug]`: 주문 확인과 토스 결제 시작
- `app/api/orders/route.ts`: 결제 전 주문 만들기
- `app/api/payments/confirm/route.ts`: 토스 결제 승인 확인
- `app/api/webhooks/toss/route.ts`: 토스가 서버로 알려주는 결제 상태 처리

### 이메일과 다운로드

- `app/api/email`: 인증번호 발송과 확인
- `emails`: 실제 이메일 문구와 디자인
- `app/api/download`: 안전한 다운로드 주소 처리
- `app/api/downloads`: 구매 파일 다시 받기
- `lib/download-links.ts`: 다운로드 주소의 만료·횟수 규칙

### 관리자 화면

- `app/admin`: 관리자 로그인과 상품·주문 관리 화면
- `app/api/admin`: 관리자 화면에서 사용하는 상품·주문·환불 기능
- `lib/admin-auth.ts`: 관리자 인증 여부 확인

### 데이터베이스와 보안

- `prisma/schema.prisma`: 현재 데이터베이스 표의 전체 모습
- `prisma/migrations`: 언제 어떤 표·보안 규칙을 변경했는지 남긴 순서별 장부
- `lib/request-security.ts`: 과도한 요청과 자동 공격 방지
- `lib/privacy.ts`: 오류 기록에 개인정보가 섞이지 않게 가리는 처리

## 주석을 넣지 않는 파일도 있습니다

`package.json`, `vercel.json`, `.prettierrc.json` 같은 JSON 파일은 규칙상 주석을 넣을 수 없습니다. 주석을 억지로 추가하면 빌드와 배포가 실패합니다. `tsconfig.json`은 예외적으로 주석을 허용하는 형식이라 파일 안에 설명을 넣었습니다.

- `package.json`: 사용 기술과 실행 명령 목록
- `tsconfig.json`: TypeScript 검사 기준
- `vercel.json`: Vercel의 매일 자동 작업 일정
- `.prettierrc.json`: 코드 줄 길이와 따옴표 같은 자동 정리 기준
- `pnpm-lock.yaml`: 설치되는 기술의 정확한 버전을 자동 기록한 파일
- `next-env.d.ts`: Next.js가 자동으로 만드는 파일이므로 직접 수정하지 않습니다.

이미지·PDF·SVG 파일은 프로그램 로직이 아니라 화면과 시험에 사용하는 자료이므로 코드 주석 대상에서 제외합니다.

## 코드를 볼 때의 안전 원칙

1. 파일을 읽는 것은 서비스에 영향을 주지 않습니다.
2. 비밀키가 들어 있는 `.env.local` 내용은 복사하거나 공유하지 않습니다.
3. 코드를 수정한 뒤에는 자동검사를 통과해야 합니다.
4. GitHub PR에서 변경 내용을 확인한 뒤 사용자가 직접 병합해야 정식 기준 코드가 바뀝니다.
5. 이해되지 않는 부분은 파일 경로와 주석 내용을 함께 전달해 질문하면 됩니다.

## 공통 컴포넌트 디자인 규칙

헤더·푸터처럼 여러 화면에 반복되는 부분은 `components` 폴더의 공통 컴포넌트를 사용합니다. 같은 컴포넌트를 사용하면서도 화면마다 다르게 보이는 일을 막기 위해 다음 기준을 따릅니다.

1. 공통 헤더와 푸터의 색상·높이·간격은 `app/globals.css`의 `.site-header`와 `footer` 규칙에서만 정합니다.
2. 개별 페이지는 `.페이지이름 .site-header`처럼 공통 디자인을 다시 덮어쓰지 않습니다.
3. 페이지별 디자인은 헤더와 푸터 사이의 본문 영역에만 적용합니다.
4. 공통 컴포넌트를 변경하면 메인·정책·재다운로드·오류 화면의 PC와 모바일 모습을 함께 확인합니다.
5. 의도적으로 다른 형태가 필요하면 기존 컴포넌트를 몰래 변경하지 않고, 이름이 다른 별도 컴포넌트로 구분합니다.
